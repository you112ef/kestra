# Minimal Dockerfile using official Kestra image as base
FROM kestra/kestra:latest

# Copy our configuration files
COPY --chown=kestra:kestra cli/src/main/resources/application-override.yml /app/confs/application-override.yml

# Set environment variables for our configuration
ENV KESTRA_CONFIGURATION='{
  "kestra": {
    "repository": {
      "type": "h2"
    },
    "storage": {
      "type": "local",
      "local": {
        "basePath": "/app/storage"
      }
    },
    "url": "http://localhost:8080/"
  },
  "micronaut": {
    "server": {
      "cors": {
        "enabled": true,
        "configurations": {
          "all": {
            "allowedOrigins": ["http://localhost:5173"]
          }
        }
      }
    }
  }
}'

# Expose the port
EXPOSE 8080

# Use the default entrypoint from the base image
CMD ["server", "local"]