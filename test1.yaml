id: test-suite-1
namespace: company.team
flowId: kookabura_411641

tests:
  - id: test-1
    type: io.kestra.core.tests.flow.UnitTest
    description: test some stuff
    fixtures:
      # Define input fixtures
      inputs:
        - id: input
          value: toto

      # Define task fixtures: a.k.a. mock of task runs and their outputs
      tasks:
        # Don't run it and set its output
        - id: output
          outputs:
            values:
              name: fake
        # Don't run it
        - id: sleep
        # Don't run it and set its state to WARNING
        - id: fail
          state: WARNING
